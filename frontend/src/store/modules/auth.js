import * as Vue from 'vue';

const types = {}
const getters = {}

const actions = {
  async loginUser({ commit }, data) {
    // let url =
  }
}

const mutations = {}
const state = {}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
